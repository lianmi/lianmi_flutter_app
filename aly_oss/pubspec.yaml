name: aly_oss
description: An aliyun oss plugin for flutter. Use this plugin to upload files to aliyun oss.
version: 1.0.1
homepage: https://github.com/lianmi/aly_oss

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # uuid: ^2.0.4
  uuid: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: tech.jitao.aly_oss
    pluginClass: AlyOssPlugin
